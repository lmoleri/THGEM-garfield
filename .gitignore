.DS_Store
build/
results/
cmake-build-*/
.idea/
.vscode/

# neBEM working directory (regenerated on every solve)
neBEMOut/

# neBEM → ComponentGrid sampled-field cache (one ~7 MB file per geometry/field point,
# regenerated automatically when missing)
field_cache/

# Magboltz property sidecar (rewritten on every run; lives next to the .gas in gas/)
*_props.csv

# ROOT outputs
*.root

# Python bytecode
**/__pycache__/
*.pyc
*.pyo
